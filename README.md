Pour éxecuter le projet, il faut lancer les commandes suivantes :

```bash
mkdir build
cd build
cmake ..
make
./ransac file.obj
```
un fichier ransac.obj sera alors générer dans le dossier build.
